# ═══════════════════════════════════════════════════════════════════════════════
# ORTHANC CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════
#
# SETUP OPTIONS:
#
#   Option 1: Interactive Setup (recommended)
#     make setup
#
#   Option 2: Quick Setup with Custom Paths
#     make quick-setup DICOM_STORAGE=/mnt/nas/dicom POSTGRES_STORAGE=/data/postgres
#
#   Option 3: Manual Setup
#     cp config/env.template .env
#     # Edit .env
#     docker compose up -d
#
# ═══════════════════════════════════════════════════════════════════════════════

# ─────────────────────────────────────────────────────────────────────────────────
# STORAGE PATHS (most commonly customized)
# ─────────────────────────────────────────────────────────────────────────────────
# These paths can be overridden during setup:
#   make setup DICOM_STORAGE=/mnt/nas/orthanc/dicom
#
# Requirements:
#   - Paths must exist before starting services
#   - DICOM_STORAGE: writable by UID 1000 (Orthanc)
#   - POSTGRES_STORAGE: writable by UID 999 (PostgreSQL)
#
# Examples:
#   Local SSD:    /opt/orthanc/orthanc-storage
#   NAS mount:    /mnt/nas/pacs/dicom
#   Docker vol:   ./data/dicom (relative to project root)

DICOM_STORAGE=/opt/orthanc/orthanc-storage
POSTGRES_STORAGE=/opt/orthanc/postgres-data

# ─────────────────────────────────────────────────────────────────────────────────
# CREDENTIALS
# ─────────────────────────────────────────────────────────────────────────────────
# The setup script will generate secure passwords if left empty
# Your original credentials are preserved below

ORTHANC_USERNAME=orthanc_admin
ORTHANC_PASSWORD=helloaide123

POSTGRES_USER=orthanc
POSTGRES_PASSWORD=

# ─────────────────────────────────────────────────────────────────────────────────
# DICOM SETTINGS
# ─────────────────────────────────────────────────────────────────────────────────
# AE Title: How this Orthanc identifies itself to other DICOM devices
# Can be overridden: make setup ORTHANC_AET=MY_PACS

ORTHANC_AET=ORTHANC_LPCH
DICOM_PORT=4242

# ─────────────────────────────────────────────────────────────────────────────────
# WEB PORTS (sequential starting at 8040)
# ─────────────────────────────────────────────────────────────────────────────────
# Adjust if you have port conflicts

OPERATOR_UI_PORT=8040      # Operator Dashboard (main entry point)
ORTHANC_WEB_PORT=8041      # Orthanc Web UI and REST API
OHIF_PORT=8042             # OHIF DICOM Viewer
POSTGRES_PORT=8043         # PostgreSQL (for external tools)
ROUTING_API_PORT=8044      # Routing Statistics API
GRAFANA_PORT=8045          # Grafana QI Dashboards

# ─────────────────────────────────────────────────────────────────────────────────
# GRAFANA CREDENTIALS
# ─────────────────────────────────────────────────────────────────────────────────
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin

# ─────────────────────────────────────────────────────────────────────────────────
# OPTIONAL: METRICS (Graphite)
# ─────────────────────────────────────────────────────────────────────────────────
# Uncomment to enable metrics export
# GRAPHITE_HOST=graphite.example.com
# GRAPHITE_PORT=2003
# GRAPHITE_PREFIX=orthanc

# ─────────────────────────────────────────────────────────────────────────────────
# TIMEZONE
# ─────────────────────────────────────────────────────────────────────────────────
TZ=America/Los_Angeles
